# Alembic Migrations

This folder is reserved for database migrations.

The current beginner-friendly version creates tables automatically with SQLAlchemy when the backend starts. When MargVedA grows, initialize Alembic here and manage schema changes with migrations instead of auto-create.
